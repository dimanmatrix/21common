
30 вопросов по 1 правильному ответу в каждом.
10 первых вопросов имеют общий характер. 
Советую повторить Er-модели и их связи, UML диаграммы, СУБД (в частности postgresql)
3 следующих будут про нормальные формы баз данных.
2 вопроса про ACID. 
далее 15 вопросов по типу "что будет получено в результате следующего запроса?". запросы были на картинках, я их не увидел (был баг платформы, который до сих пор не починили), но судя по ответам не очень сложные. 

я сдал на 18/30, это 67%. если отталкиваться, что первые 15 были отвечены правильно, то из последних 15 (которых я не видел) я угадал 3. этого было достаточно.


================================================
================================================
================================================


Дополню по темам:
1. Er-модели. Отношения, атрибуты, связи.
2. Реляционные и нереляционные СУБД. Примеры. 
3. ACID и CAP.
4. CHECK Constraint, индексы, ключи.

В блоке из 15 вопросов с запросами встречалось следующее:
1. NULL в запросах;
2. JOIN (LEFT, RIGHT, FULL, CROSS, NATURAL), UNION, INTERSECT;
3. NULL FIRST, NULL LAST при сортировке;
4. SELECT DISTINCT, LIMIT, NOT EXISTS;
5. ROW_NUMBER, OVER.


================================================
================================================
================================================


Это часть моих вопросов в первой и единственной попытке экзамена SQL. 
Проходной балл там низкий, чето 50% примерно, но оценка не линейно идет, 
у меня было 15\30 баллов расценено как 60%. 

Запросы сами не напишу уже, но там есть нюансики, в которых можно тупить, 
но в целом ок все. 

Например, я чет сомневалась над сортировкой, 
если написано nulls last, в каком порядке тогда остальные данные будут стоять. 


Что такое атрибуты в ER-модели?
- характеристики сущности
- ключевые слова
- отношения между сущностями

1 нормальная форма - характеристики:
- каждая таблица имеет первичный ключ
- каждая ячейка содержит только 1 значение
- все атрибуты зависят от первичного ключа
- все значения атрибутов в таблице уникальны 

Что такое первичный ключ в физической модели данных?
- столбец, содержащий внешние ключи
- столбец, содержащий уникальные значения для каждой строки с нулл-значением
- столбец, содержащий дублирующиеся значения
- уникальный идентификатор записи в таблице


Какая DDL-команда используется для создания check constraint? 
- insert into
- add constraint
- alter view
- alter table 

Какая команда используется для создания Foreign Key? 
- insert into
- add constraint
- alter view
- create index 

Термин Реляционная база данных 
- структура, основанная на графах
- структура, основанная на хранилище JSON-файлов
- структура, основанная на больших файлах
- структура, основанная на связях между таблицами 

Какой из языков обычно используется для описания логических моделей данных?
- json
-uml
-html
-sql

Какое из понятий описывает связь м\у двумя сущностями в логической модели данных 
- Хем-код
- Триггер
- внешний ключ
- индекс
Что если check constraints не пройдет при вставке\обновлении данных?
- ограничение будет отключено
- данные будут удалены
- операция вставки\обновления будет отклонена
- бд выдаст предупреждение, но продолжит операцию 

Какая DDL- команда поможет создать составной индекс в реляционной базе данных?
- create index table_name (col1), index (col2)
- add composite index table_name(col1, col2)
- create index composite_index ON table_name(col1, col2)
- create composite index table_name(col1, col2)

Какая служба управляет доступом к данным в реляционной бд?
- Query optimizer
- Database Management System (DBMS)
- SQL server
- transaction manager


Select 1+1 AS value WHERE 1=1 AND 2=1
- запрос ничего не вернет
- null 
- 2
- синтаксическая ошибка

Select sum(t1.value) from (select null::integer AS value) as t1;
- 1
- 1
- null
- синтаксическая ошибка

SELECT case when null =5 THEN ‘A’
WHEN NULL <>5 THEN ‘B’
else ‘C’
and AS result;
- A
- B
- Null
- C


================================================
================================================
================================================


из подвохов еще советую почитать про 
1. ORDER BY int, где int не является именем столбца
2. string_agg

